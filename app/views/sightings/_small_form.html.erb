<%= form_for(sighting) do |f| %>
  <% if sighting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sighting.errors.count, "error") %> prohibited this sighting from being saved:</h2>

      <ul>
      <% sighting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_field :date %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.time_field :time %>
  </div>
  <div class="field">
    <%= f.label :animal_id %><br>
    <%= select(:sighting,:animal_id, animals) %>
  </div>
    <div class="field">
    <%= f.label :region %><br>
    <%=  f.select:region,['safari', 'Madrid', 'Africa'],:prompt => 'Select One'%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <div class="close">
      <button>Close</button>
  </div>
  <% end %>
